@model IEnumerable<LIDOM_MVC.Models.Partido>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParJuego)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParEquipoGanador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParEquipoPerdedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ParJuego)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParEquipoGanador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParEquipoPerdedor)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.ParId }) |
                    @Html.ActionLink("Details", "Details", new { id=item.ParId }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.ParId })
            </td>
        </tr>
}
    </tbody>
</table>
